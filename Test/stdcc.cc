/* stdcc.c */
#include "stdcc.h"

/* remarks */
int print(const char* s) { return printf(s); }

/* remarks */
int println(const char *s) { return puts(s); }

/* remarks */
int Write(const char* s) { return printf(s); }

/* remarks */
int WriteLine(const char *s) { return puts(s); }
